import { PreventPasteDirective } from './prevent-paste.directive';

describe('PreventPasteDirective', () => {
    it('should create an instance', () => {
        const directive = new PreventPasteDirective();
        expect(directive).toBeTruthy();
    });
});
