import { MaxNumbersTypeDirective } from './max-numbers-type.directive';

describe('MaxNumbersTypeDirective', () => {
    it('should create an instance', () => {
        const directive = new MaxNumbersTypeDirective();
        expect(directive).toBeTruthy();
    });
});
